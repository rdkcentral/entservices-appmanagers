set (autostart ${PLUGIN_APP_STORAGE_MANAGER_AUTOSTART})
set (preconditions Platform)
set (callsign "org.rdk.AppStorageManager")

if(PLUGIN_APP_STORAGE_MANAGER_STARTUPORDER)
set (startuporder ${PLUGIN_APP_STORAGE_MANAGER_STARTUPORDER})
endif()

map()
   key(root)
   map()
       kv(mode ${PLUGIN_APP_STORAGE_MANAGER_MODE})
       kv(locator lib${PLUGIN_IMPLEMENTATION}.so)
   end()
    kv(path ${PLUGIN_APP_STORAGE_MANAGER_PATH})
end()
ans(configuration)
